using Gtk 4.0;
using Adw 1;

template $MessageBubble: Gtk.Box  {
    orientation: vertical;

    Label sender_label {
        styles ["caption-heading"]

        ellipsize: end;
        xalign: 0;
    }

    Adw.Bin message_reply_bin {}

    Adw.Bin prefix_bin {}

    Label message_label {
    }
}
